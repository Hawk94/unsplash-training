import React from 'react';
import './Card.css';

const Card = ({
  id,
  createdAt,
  description,
  userId,
  userLocation,
  imageColor,
  imageUrl
}) => {
  return <div className="Card">{/* Your presentational logic here. */}</div>;
};
